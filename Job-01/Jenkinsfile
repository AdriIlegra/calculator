pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                sh './gradlew clean build'
            }
        }

        stage('Artifactory') {
            steps {
                rtUpload(
                    serverId: "artifactory",
                    spec: """{
                        "files": [
                        {
                            "pattern": "build/libs/tema-06-0.0.1-SNAPSHOT.jar",
                            "target": "calculadora/"
                        }
                        ]
                    } """


                    )
            }
        }

    }
}