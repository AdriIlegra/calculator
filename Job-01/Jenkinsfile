pipeline {
    agent any

    stages {
        stage('Upload to Artifactory') {
            steps {
                script {
                    def server = Artifactory.server 'artfactory-admin'
                    def uploadSpec = """{
                        "files": [
                            {
                                "pattern": "build/libs/*.jar",
                                "target": "caluladora/"
                            }
                        ]
                    }"""
                    server.upload(uploadSpec)
                }
            }
        }
    }
}
